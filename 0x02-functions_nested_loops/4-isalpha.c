#include "holberton.h"
/**
 * islower - checks if letter is lowercase
 * @c: checks to see if char is lowercase
 * Return: Always 0
 */
int main(void)
{
    if (c >= 'a' && c <= 'z')
    {
        return (1);
    }
    else if (c >= 'A' && c <= 'Z')
    {
        return (1);
    }
    else
    {
        return(0);
    }
}
